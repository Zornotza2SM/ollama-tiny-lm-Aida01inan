version: '3.8'

services:
  ollama:
    image: ollama/ollama:latest
    container_name: mi_ia_local
    ports:
      - "11434:11434"
    volumes:
      - ./ollama_data:/root/.ollama
    environment:
      - OLLAMA_ORIGINS=*
    restart: always